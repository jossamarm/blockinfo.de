## Installation

```
bundle install
```

## Runing locally

```
$ jekyll s --incremental
```

## Deploy a new version

Install it first:

    gem install jgd

Commit & Push all changes. Current remote master we be build.
Run it locally:

    jgd
